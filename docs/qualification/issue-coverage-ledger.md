# Issue Coverage Ledger

**Evidence contract:** `docs/qualification/README.md`. This ledger contains rows created before the full Evidence Receipt schema existed; treat each row as scoped by its linked trace/test rather than assuming one shared environment. New or materially revised rows should name enough scope/revision/invalidation information to prevent accidental reuse.

| Issue | Classification | Failure family | Reproduced | First failing boundary | Test/fixture | Disposition | Release blocker | Evidence gap |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| #199 | Confirmed defect | Output schema validation | Yes | Zod outputSchema | `tests/browser-json-schema-validator.test.ts` | Fixed | Yes | Live-site insertion test |
| Discovery Failure | Confirmed defect | Truthful connection state | Yes | `StreamableHttpPlugin.getPrimitives()` | `tests/mcp-client-discovery-state.test.ts` | Fixed | Yes | Live-site error UI handling |
| Execution↔delivery correlation | Confirmed gap (static, see `round-trip-baseline.md`) | Operation identity | N/A (static) | Identity dropped at renderer→client and completion-event seams | Type-check + node suite after September 12 attempt-identity change; focused MCP-client and operation-observation regressions | **Implemented at source/node-test level (Stage 1)** — logical `callId` crosses the explicit path; each concrete dispatch has a distinct `attemptId` preserved into MCP-client events and operation observation; ContextBridge tool calls are single-dispatch | No | Live-browser correlation/attempt confirmation without implying exactly-once server effects |
| Delivery receipt/recovery | Confirmed gap (static, see `round-trip-baseline.md`) | Delivery evidence | Yes (source + focused node regression, 2026-09-13) | Insert/submit originally shared one mutable receipt outcome | `tests/delivery-checkpoint-recovery.test.ts`; Chrome-extension node suite; type-check | **Implemented at source/node-test level (Stage 2)** — bounded receipts persist C5 delivery and C6 submission independently; failed C6 recovery resumes at submission with zero additional insertion and no MCP execution path | No for checkpoint-collapse defect | Live-browser confirmation of insertion/submission recovery and page-state truth |
| Unified operation snapshot | Confirmed gap (Observability Map 3) | Observation | Yes (source + focused node regression, 2026-09-13) | Status/uncertainty/blocker/next-action required log reconstruction across owners | `tests/operation-observation-safety.test.ts`; `tests/operation-observation-effect-safety.test.ts`; `tests/delivery-checkpoint-recovery.test.ts`; source re-audit of `operation-observation.ts` | **Partial (Stage 3)** — snapshot exposes attempt IDs; ambiguous dispatch remains C3 `unknown`; confirmed C5 plus failed C6 is represented independently and routes to submission recovery; formal action admission is still absent | Yes | Live observation confirmation; explicit effect-class/action-admission contract |
| Continuation bounds / stale-action continuation | Confirmed gap (plan Stage 4) | Continuation bounds + effect safety | Yes (focused regression, 2026-09-12) | Delivery retry originally had no cap; generic ContextBridge retry could redispatch `mcp:call-tool` after timeout | `tests/context-bridge-retry-safety.test.ts`; `tests/operation-observation-effect-safety.test.ts`; MCP-client attempt regression | **Partial (Stage 4)** — delivery retry/destination/preference bounds and hard single-dispatch tool messaging exist; concrete attempts are observable and ambiguous dispatch routes to reconcile/`stop` | Yes | Live delay/preference/interruption confirmation plus formal effect-class/action-admission evidence |
| Documentation tower accretion | Plan Stage 5 acceptance | Evidence reuse | docs-only walk-through | Unverified whether scoped evidence alone resolves the canonical delivery scenario | Cold-reader Q-to-A map recorded; 4 retrieval/scope/invalidation failures found and repaired in place | Demonstrated (evidence level) — independent agent run outstanding | Yes (accretion-demonstration.md) | Independent agent resolves the scenario from the scoped evidence set alone |
| Agent control/evidence contract | Plan Stage 6 | Agent cognition + evidence lifecycle | Documentation audit, 2026-09-10 | Multiple adjacent working abstractions (Task Capsule, operation observation, resume summary, qualification files) lacked one shared situation/evidence lifecycle | Core docs now use one Frame-first Situation Packet; schema-2 typed-interface topology; versionable documentation/evidence paths; Evidence Receipts define certainty, invalidation, and promotion | Addressed at documentation/design level; current receipt remains scoped pending cold-agent validation | No | Independent cold-agent test; implementation of derived one-shot inspection remains proposed Stage 7 |
| MCP response type/runtime drift | Static contract audit, 2026-09-10 | Shared message contract | Yes (source inspection) | `messages.ts` models wrapper responses for `mcp:call-tool`, `mcp:get-tools`, and `mcp:get-server-config`, while the background places bare result/array/config values in the response payload and content callers consume those bare values | Source inspection of `types/messages.ts`, `background/index.ts`, `core/context-bridge.ts`, `core/mcp-client.ts` | Open contract debt; runtime may work because callers bypass the declared wrappers, but type safety is misleading | No | Choose one runtime shape, align message map + producers + consumers together, then type-check both sides |
| `mcp:tool-update` broadcast shape mismatch | Confirmed defect (static), 2026-09-10 | Shared message contract / discovery projection | Yes (source inspection) | Background broadcasts `payload: { tools }` matching `ToolUpdateBroadcast`; content listener accepts only `Array.isArray(message.payload)` and otherwise substitutes `[]` | Source inspection of `background/index.ts` and content `core/mcp-client.ts` | Open | Yes | Add producer/consumer regression test; align listener with typed broadcast and verify live update does not clear tools |
| Hidden `mcp:call-tool` redispatch | Confirmed safety gap, repaired in current working tree 2026-09-12 | Operation attempt + effect safety | Yes (source inspection + timeout regression) | Generic `ContextBridge.sendMessage()` previously retried timeout/retryable failures, creating a new message ID and another background/server invocation | `tests/context-bridge-retry-safety.test.ts`; content `core/context-bridge.ts` | **Fixed for implicit redispatch** — `mcp:call-tool` is hard single-dispatch even if generic retries are requested; distinct attempt evidence is tracked separately from logical operation identity | No for this defect | Live browser timeout confirmation; exactly-once server effects remain outside this claim |
| Delivery checkpoint collapse after submit failure | Confirmed defect, repaired in current working tree 2026-09-13 | Monotonic delivery evidence | Yes (source inspection + focused regression) | Successful C5 wrote `stage=delivered`; C6 failure overwrote the same receipt with `stage=failed`; recovery then called insert before submit | `tests/delivery-checkpoint-recovery.test.ts`; source inspection of `delivery-recovery.ts`, `automation.service.ts`, `operation-observation.ts`; Chrome-extension node suite + type-check | **Fixed at source/node-test level** — C6 failure is separate evidence, C5 remains confirmed, observation reports `submission-failed`, and retry invokes submission without another insertion or MCP execution path | No for this defect | Live-browser confirmation of submit-failure recovery/page-state outcome |
| Same-input page mutation overlap | Confirmed concurrency gap, repaired in current working tree 2026-09-13 | Delivery ordering / shared input mutation | Yes (focused concurrent regression) | Concurrent completion handlers and `retryDelivery()` could enter insert/submit mutation paths at the same time against the active page input | `tests/delivery-checkpoint-recovery.test.ts`; source inspection of `automation.service.ts`; Chrome-extension node suite; type-check | **Fixed at source/node-test level (Stage 9 serialization slice)** — normal completion and delivery retry enter one FIFO page-mutation queue; destination is captured before queueing; the regression holds the first insertion open and proves a second delivery cannot enter concurrently | No for this concurrency defect | Live-browser same-input confirmation; adapter acknowledgement remains distinct from verified page state |
| Adapter acknowledgement vs verified page state | Confirmed Stage 9 evidence gap, repaired in current working tree 2026-09-13 | Delivery truth / page-state verification | Yes (source inspection + focused regression) | Adapter success previously promoted directly to C5 `delivered` without independent page observation | `tests/delivery-checkpoint-recovery.test.ts`; source inspection of `automation.service.ts`, adapter contract/base verifier, `delivery-recovery.ts`, and `operation-observation.ts`; Chrome-extension node suite + type-check | **Fixed at source/node-test level (Stage 9 page-verification slice)** — adapter acknowledgement is recorded as `acknowledged` unless page observation verifies the expected text; only verified state promotes C5 to confirmed, and acknowledgement-only recovery reconciles page state before any reinsertion | No for this source-level gap | Site-specific verification remains unqualified in live browsers; generic verification is deliberately conservative when the exact target cannot be proven |

## Current Stage 6 receipt metadata

| Field | Value |
|---|---|
| `receipt_id` | `AGENT-CONTROL-2026-09-11-1` |
| `claim` | The documentation tower defines one Frame-first Situation Packet, one static bidirectional interface topology, and one scoped Evidence Receipt lifecycle without introducing a mutable agent-state store; operation facets are agent-facing lenses over narrowly owned source facts, derived projections preserve provenance, outcome remains an acceptance judgment, and neither observation nor the proposed Situation Compiler becomes a competing fact owner |
| `owner` | `CONTROL_DOCUMENTATION` |
| `scope` | Repository documentation and qualification contracts; no runtime implementation or browser qualification |
| `revision` | HEAD `be60d935159a` plus the current 2026-09-11 working-tree changes |
| `evidence` | Direct re-read of the affected control-model passages in `AGENTS.md`, `SYSTEM.md`, `ARCHITECTURE.md`, `AGENT_GUIDE.md`, `README.md`, `DIAGNOSIS.md`, `docs/agent-control-map.yaml`, and this ledger; HEAD `be60d935159a`; file-level whitespace, YAML parse, core local-reference, and targeted contradiction/status checks |
| `status` | `verified-scoped` — directly verified for the current documentation/topology working tree only; this does not satisfy the independent cold-agent ergonomics acceptance or qualify runtime behavior |
| `uncertainty` | Independent cold retrieval, broader non-core documentation link audit, and implementation of the Situation Compiler remain open; runtime Stages 1–4 have newly identified gaps and are not covered by this documentation-only receipt |
| `invalidates_on` | Control-loop/schema/topology changes, documentation routing changes, qualification receipt-schema changes, or a contradiction found by cold-agent testing |
| `supersedes` | `AGENT-CONTROL-2026-09-10-4` — narrows the ownership model by distinguishing atomic source ownership from composed operation lenses; older receipts remain superseded transitively as provenance |
