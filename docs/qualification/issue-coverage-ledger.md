# MCP SuperAssistant Issue Coverage Ledger

Status: v0.6.3-rc.6 package/protocol qualification ledger. Browser runtime remains unqualified unless a row explicitly records a browser run.

This ledger tracks issue-family coverage by first failing boundary and evidence, not by broad symptom similarity. Do not mark an issue or cluster fixed unless its distinctive acceptance criteria were reproduced and covered by a named test or browser run.

## Evidence labels

- **Verified**: directly confirmed from this checkout, command output, source inspection, test output, packaged artifact inspection, or real-browser run.
- **Claimed**: stated by an issue, release note, status document, or reporter, but not independently reproduced in this qualification pass.
- **Inferred**: supported by source inspection or consistent project evidence, but not runtime-verified.
- **Unknown**: not yet inspected or not enough evidence.

## Ledger

| Issue | Classification | Failure family | Reproduced | Component | First failing boundary | Test/fixture | Disposition | Release blocker | Evidence gap |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| #199 | Claimed fixed, needs preservation | Schema/discovery/CSP compatibility | Claimed by `STABILIZATION_STATUS.md`; not rerun in this pass | MCP SDK validation / schema adapter / extension CSP | Claimed around MCP schema validation and CSP-safe output schema handling | Existing schema-related deterministic tests; exact test names to be preserved | Preserve coverage; do not regress while changing discovery state | Yes | Rerun current tests; keep valid `outputSchema`; record test output |
| #196 | Plausible defect needing reproduction | Discovery reports connected/zero tools after failure | Not yet reproduced by test in this pass | Streamable HTTP plugin / `McpClient` / UI state | Inferred: discovery failure after connection can leave healthy connection state | Required new regression: advertised tools + `tools/list` failure | Immediate tranche target | Yes | Add failing test and verify old behavior fails for expected reason |
| #191 | Plausible defect needing reproduction | Discovery/config/framing variant | Not yet reproduced | Transport/plugin/client state | Unknown | None yet | Cluster for reproduction before claiming fix | Yes if matching declared support | Refresh issue facts and create distinctive fixture if needed |
| #176 | Plausible defect needing reproduction | Discovery/config/framing variant | Not yet reproduced | Transport/plugin/client state | Unknown | None yet | Cluster for reproduction before claiming fix | Yes if matching declared support | Refresh issue facts and create distinctive fixture if needed |
| #171 | Plausible defect needing reproduction | Discovery/config/framing variant | Not yet reproduced | Transport/plugin/client state | Unknown | None yet | Cluster for reproduction before claiming fix | Yes if matching declared support | Refresh issue facts and create distinctive fixture if needed |
| #158 | Plausible defect needing reproduction | Discovery/config/framing variant | Not yet reproduced | Transport/plugin/client state | Unknown | None yet | Cluster for reproduction before claiming fix | Yes if matching declared support | Refresh issue facts and create distinctive fixture if needed |
| #87 | Plausible defect needing reproduction | Discovery/config/framing variant | Not yet reproduced | Transport/plugin/client state | Unknown | None yet | Cluster for reproduction before claiming fix | Yes if matching declared support | Refresh issue facts and create distinctive fixture if needed |
| #12 | Plausible defect needing reproduction | Discovery/config/framing variant | Not yet reproduced | Transport/plugin/client state | Unknown | None yet | Cluster for reproduction before claiming fix | Yes if matching declared support | Refresh issue facts and create distinctive fixture if needed |
| Streamable HTTP JSON framing | Verified package/protocol coverage | Transport response framing | Verified by `pnpm -F chrome-extension test` and root E2E fixture tests for v0.6.3-rc.6 | Streamable HTTP plugin | Response framing parser / SDK boundary | `chrome-extension/tests/streamable-http-framing.test.ts`; `tests/e2e/fixture-server.test.mjs` | Preserve coverage | No for package/protocol; browser runtime still separate | Live browser transport execution not run |
| Streamable HTTP SSE-framed POST | Verified package/protocol coverage | Transport response framing | Verified by `pnpm -F chrome-extension test` and root E2E fixture tests for v0.6.3-rc.6 | Streamable HTTP plugin | SSE event parsing / JSON-RPC response handling | `chrome-extension/tests/streamable-http-framing.test.ts`; `tests/e2e/fixture-server.test.mjs` | Preserve coverage | No for package/protocol; browser runtime still separate | Live browser transport execution not run |
| Advertised tools but `tools/list` fails | Verified deterministic regression | Truthful discovery state | Verified by discovery-state tests in v0.6.3-rc.6 pass | `McpClient`, Streamable HTTP plugin, sidebar/background callers | `getPrimitives()` failure path clears primitive cache, marks disconnected, emits `Primitive discovery failed`, and rethrows | `chrome-extension/tests/mcp-client-discovery-state.test.ts` | Fixed at `McpClient` state/cache boundary for deterministic test scope | No for package/protocol; browser runtime still separate | Chrome/Firefox chat.qwen.ai runtime not run |
| Stale cache followed by discovery failure | Verified deterministic regression | Cache invalidation/truthful degraded state | Verified by discovery-state tests in v0.6.3-rc.6 pass | `McpClient` primitive cache and UI state | Cache/discovery boundary | `chrome-extension/tests/mcp-client-discovery-state.test.ts` | Fixed at cache/state boundary for deterministic test scope | No for package/protocol; browser runtime still separate | Chrome/Firefox UI runtime not run |
| Qwen JSONL function-block extraction | Verified deterministic parser regression | JSONL function-call rendering/execution | Reproduced with compact polluted Qwen-style JSONL: detection found JSON calls, but extraction returned null function name/call ID before the rc4 patch. rc5 also covers polluted partial starts. | Content renderer parser | Robust detection used normalized JSON objects, but downstream extraction used newline-only parsing and polluted partial starts could still fall back to `function` / generated `block-*` | `chrome-extension/tests/json-function-parser.test.ts`; manual parser reproduction | Fixed for deterministic parser scope by normalizing JSON object candidates and trimming polluted text before partial start extraction | No for parser scope; browser runtime still separate | Live Qwen model output/browser DOM confirmation still required |
| Qwen incomplete function-block spinner | Source-path verified candidate fix | JSONL streaming/rendering bounded failure | User reported persistent live spinner after rc4. Source inspection showed `mutationObserver.ts` monitored a pre-render block ID that could differ from the `data-block-id` assigned by `renderFunctionCall()`, so `monitorNode()` could miss the rendered loading block. | Content renderer observer | Mutation observer to stream monitor ID handoff | Source-path inspection; rc5 package build/E2E; no live DOM run | Fixed candidate: monitor the actual rendered block ID so existing abrupt-end handling can find and bound incomplete function blocks | Yes for browser confidence | Live Qwen runtime confirmation still required |
| Qwen instruction template | Verified deterministic instruction regression | Model instruction/output shaping | User diagnostics showed live Qwen copied `<thoughts>` and emitted only `function_call_start`. The inspected generic template contained `<thoughts>`/response-format scaffolding, manual-execution wording, JSON-array wording, and schema-inaccurate `parameters object` phrasing. | Instruction generator | Host-specific instruction generation | `chrome-extension/tests/instruction-generator-qwen.test.ts` | Fixed for deterministic instruction scope: Qwen hosts receive strict JSONL-only instructions without `<thoughts>`, manual-execution wording, JSON-array wording, or XML-style custom tags; non-Qwen generic behavior is explicitly preserved by test | Yes for browser confidence | Live Qwen confirmation still required |
| Root `pnpm e2e`/`pnpm e2e:firefox` | Verified package/protocol harness | E2E false green / fixture harness | Verified `pnpm e2e` PASS 15/15 and `pnpm e2e:firefox` PASS 15/15 for v0.6.3-rc.6 | CI / package scripts / test harness | Root scripts rebuild artifacts and execute deterministic package/protocol tests, including numeric manifest version and manifest icon existence assertions | `tests/e2e/e2e.test.mjs`; `tests/e2e/fixture-server.test.mjs` | Nonzero package/protocol E2E restored; redundant raw reachability check removed; manifest prerelease regression guarded | No for package/protocol; browser E2E/manual runtime remains | No live browser load or site adapter execution in these tests |
| Manifest referenced icons | Verified package/archive regression | Chrome load blocked before runtime | Reproduced from user Chrome error and package inspection: `icon-16.png` was referenced but absent in v0.6.3-rc.2; preserved in v0.6.3-rc.6 | Manifest/public assets/package contract | Manifest icon path to packaged asset boundary | `tests/e2e/e2e.test.mjs`; `docs/qualification/package-baseline-v0.6.3-rc.6.md` | Fixed for package scope by adding `chrome-extension/public/icon-16.png` and asserting all manifest icons exist in `dist` | No for package/protocol; browser runtime still separate | Live Chrome load of final ZIP still needs manual confirmation |
| Chrome packaged runtime | Package verified; runtime not run for v0.6.3-rc.6 | Browser/package qualification | Package/archive inspected; manifest preflight confirms numeric `version` and all declared icons exist; not loaded in Chrome in this pass | Build/package/browser | Package manifest/archive boundary | `docs/qualification/package-baseline-v0.6.3-rc.6.md`; final ZIP hash recorded | Runtime qualification required before claiming Qwen fix in-browser | Yes for release promotion beyond RC | Load final ZIP in Chrome/Chromium, capture background/page logs, test Qwen SSE and Streamable HTTP |
| Firefox packaged runtime | Package verified; runtime not run for v0.6.3-rc.6 | Browser/package qualification | Package/archive inspected; manifest preflight confirms numeric `version` and all declared icons exist; not loaded in Firefox in this pass | Build/package/browser | Package manifest/archive boundary | `docs/qualification/package-baseline-v0.6.3-rc.6.md`; final XPI hash recorded | Runtime qualification required before claiming Firefox behavior | Yes for release promotion beyond RC | Load final XPI in Firefox, capture background/page logs, test Qwen SSE and Streamable HTTP |
| Supported-site insertion/submission | Unknown | Site adapter contract | Not run in this pass | Content script/site adapter/host DOM | Unknown | None current | Deferred until adapter contract work | Yes for advertised qualified sites | Choose qualified sites; run deterministic fixture/manual browser protocol |
| Reconnect/cancellation/exactly-once | Unknown | Lifecycle/result delivery | Not run in this pass | Transport/client/background/content messaging | Unknown | None current | Deferred work package | Yes | Add deterministic fixture scenarios and exactly-once assertions |
| Large or unsupported results | Unknown | Payload safety/result rendering | Not run in this pass | Tool result normalization/rendering/content insertion | Unknown | None current | Deferred work package | Yes | Define budgets and tests for large text/JSON/base64/unsupported content |
| Security/privacy/permissions | Package scan verified; full review still open | Security/privacy | Packaged runtime-code-generation token scan clean for final v0.6.3-rc.6 ZIP/XPI; full security review not run | Manifest/background/content/package bundle | Package artifact boundary | `docs/qualification/package-baseline-v0.6.3-rc.6.md` | Preserve strict CSP/permissions; complete full security/privacy review before stable release | Yes for full release | Full background/content message validation, storage, rendering, and permission review still pending |

## Current release-blocking gaps

1. Chrome runtime verification on `chat.qwen.ai` has not been run for final `v0.6.3-rc.6` artifacts.
2. Firefox runtime verification on `chat.qwen.ai` has not been run for final `v0.6.3-rc.6` artifacts.
3. Live browser execution for both SSE and Streamable HTTP remains unverified; deterministic package/protocol tests are not a substitute for runtime qualification.
4. Supported-site adapter contract verification, lifecycle/exactly-once browser coverage, payload budget coverage, and full security/privacy review remain incomplete.
5. Repository-wide `pnpm -F chrome-extension lint` still fails on broad baseline/pre-existing lint debt; only targeted lint for the edited discovery-state test passed.

## Progress update after T0-T4 and v0.6.3-rc.6 packaging

The initial baseline findings are retained below for traceability. The following changes were verified after that baseline was written:

- **Verified**: `chrome-extension/tests/mcp-client-discovery-state.test.ts` now covers primitive discovery failure while transport health remains true and stale-cache reuse after forced discovery failure. The red run failed for the intended assertions before the code fix.
- **Verified**: `chrome-extension/src/mcpclient/core/McpClient.ts` now clears the primitive cache, marks the client disconnected, emits `Primitive discovery failed`, and rethrows the original error when primitive discovery fails.
- **Verified**: `pnpm -F chrome-extension test` passed 7/7 after the fix, preserving the existing issue #199/schema tests and Streamable HTTP JSON/SSE-framed discovery tests.
- **Verified**: `pnpm -F chrome-extension type-check` passed after the fix.
- **Verified**: `docs/qualification/package-baseline-v0.6.2-rc.1.md` records Chrome and Firefox package commands, archive paths, SHA-256 hashes, manifest summaries, and build warnings for the current candidate.
- **Verified**: `docs/qualification/e2e-harness-plan.md` documents the current no-op root E2E risk and bounded deterministic fixture-backed harness follow-up.
- **Partially verified**: Chrome runtime now has first manual browser evidence — extension loaded, filesystem MCP tool calls completed on Z.ai, Qwen AI, and Gemini. Still needs guided browser protocol for full flow verification.
- **Verified**: `chrome-extension/tests/mcp-client-discovery-state.test.ts` now also covers reconnect after primitive discovery failure, including cleanup of stale connection state before reconnecting and successful tool execution after reconnect.
- **Verified**: `McpClient.performConnection()` now clears stale client/plugin/transport/cache state before every new connection attempt, even when `isConnectedFlag` is already false after discovery failure.
- **Verified**: successful MCP connection setup now uses the shared timeout helper so the connection timeout timer is cleared after success.
- **Verified**: `pnpm -F chrome-extension test` passed 19/19, `pnpm -F chrome-extension type-check` passed, and targeted ESLint for `chrome-extension/tests/mcp-client-discovery-state.test.ts`, `chrome-extension/manifest.ts`, `chrome-extension/tests/json-function-parser.test.ts`, and `chrome-extension/tests/instruction-generator-qwen.test.ts` passed across the v0.6.3-rc.6 change set.
- **Verified**: `pnpm e2e` and `pnpm e2e:firefox` each passed 15/15 deterministic package/protocol tests after removing the redundant raw fixture reachability check from `tests/e2e/e2e.test.mjs`.
- **Verified**: `docs/qualification/package-baseline-v0.6.3-rc.6.md` records final Chrome ZIP and Firefox XPI paths, SHA-256 hashes, manifest summaries, archive integrity, packaged runtime-code-generation token scan results, numeric manifest `version`/`version_name` release-label evidence, and manifest icon presence for v0.6.3-rc.6.
- **Verified**: `v0.6.3-rc.3` adds `chrome-extension/public/icon-16.png` and package-contract coverage that fails if a generated manifest icon path is missing from `dist`, addressing the Chrome load blocker found in `v0.6.3-rc.2`.
- **Verified**: `v0.6.3-rc.4` adds deterministic parser coverage for compact/noisy Qwen-style JSONL and uses normalized JSON object candidates for function name, call ID, description, and parameter extraction.
- **Verified**: `v0.6.3-rc.5` adds deterministic parser coverage for polluted partial JSON starts and fixes the renderer observer handoff to monitor the actual rendered block ID, bounding incomplete JSON function-block spinners in the inspected source path.
- **Verified**: `v0.6.3-rc.6` adds deterministic instruction-generator coverage for Qwen-specific strict JSONL instructions that omit `<thoughts>`, manual-execution wording, JSON-array wording, and XML-style custom tags while preserving non-Qwen generic behavior.
- **Still release-blocking**: Chrome/Firefox runtime qualification on `chat.qwen.ai` for both SSE and Streamable HTTP, supported-site adapter contract verification beyond deterministic fixtures, lifecycle/exactly-once browser coverage, payload budget coverage, and full security/privacy review remain incomplete.

## Update rules

- Add one row per issue or distinctive failure scenario.
- Keep duplicate-looking symptoms separate until their first failing boundary is proven identical.
- Record exact test file, fixture scenario, command output, artifact hash, or browser evidence before changing `Reproduced` to Verified.
- If a capability is downgraded from qualified to experimental, update the support matrix and known limitations in the same change set.
